## What

## Why

## Checklist
- [ ] `go test ./...` passes in `backend/`
- [ ] `npm run build` passes in `frontend/` (if console touched)
- [ ] Design docs updated if behavior diverges (docs/design/…)
- [ ] Migrations are additive (roadmap invariant 2)
